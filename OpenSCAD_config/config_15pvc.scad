// pipe
outerDiameter = 15.3;     
innerDiameter = 13.3;

//air tube
tube = 6.5;          // should be at least as thick as screw head

// screw
screwDiameter = 1.9;
screwHeadDiameter = 4; // be a bit generous here / round up
screwHeadHeight = 1.3;

// pipe specifics
minWallThickness = 0.8;
minAirway = 1.5;
flueWidth = 0.3;