// pipe
outerDiameter = 60;     
innerDiameter = 55.1;

//air tube
tube = 18;          // should be at least as thick as screw head

// screw
screwDiameter = 7.9;
screwHeadDiameter = 15; // be a bit generous here / round up
screwHeadHeight = 5.7;

// pipe specifics
minWallThickness = 1.2;
minAirway = 3;
flueWidth = 0.3;