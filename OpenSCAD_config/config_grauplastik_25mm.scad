// pipe
outerDiameter = 25;
innerDiameter = 22.5;

//air tube
tube = 8;          // should be at least as thick as screw head

// screw
screwDiameter = 3;
screwHeadDiameter = 6; // be a bit generous here / round up
screwHeadHeight = 2;

// pipe specefics
minWallThickness = 0.8;
minAirway = 2.5;
flueWidth = 0.3;